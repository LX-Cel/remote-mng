# 通用远程开发领域术语

本项目主要向 Agent 提供远程命令、持续交互、文件传输和作业管理能力。用户用自然语言向已有 Agent 提出目标；Agent 优先调用本地 CLI，MCP 是可选入口。业务命令与结果规则由项目资料、调用者或交互配置定义。

## Language

**Agent CLI**：由 Agent 通过宿主终端能力调用的命令接口。提供可发现的帮助、机器可读结果以及任务和会话标识，不要求终端用户记住命令。
_Avoid_: “CLI 只给人用，MCP 才给 Agent 用”

**远程目标**：具有稳定标识的远程操作对象，关联连接入口和执行环境。同一目标的终端连接与文件传输可以使用不同入口。
_Avoid_: 多目标操作中未指明对象的“远端”

**远程连接**：通往目标的一次 SSH 或 Telnet 通信连接。连接在线与远程程序仍在运行分别描述。
_Avoid_: 将连接中断等同于任务失败

**交互会话**：允许连续输入、读取输出的远程终端上下文。当前命令环境可以是操作系统 Shell，也可以是应用交互前台。
_Avoid_: 将所有交互会话统称为 Shell

**交互配置**：调用者为某类交互程序定义的进入、退出、提示符与输出判据等约定。它不代表工具理解程序的全部业务语义。
_Avoid_: 硬编码的业务专用连接方式

**受管作业**：由工具启动、以稳定标识跟踪的远程进程工作，具有可查询的执行状态和输出证据。
_Avoid_: 将任意已发送的终端输入称为可恢复作业

**产物**：需要传输或部署的具体文件或目录，以及可获得的版本、来源和校验信息。
_Avoid_: 将源码版本、待上传文件和远端运行版本混称为当前版本

**部署流程**：调用者定义的产物传输、远程操作及验证步骤组合。步骤成功与业务验证通过分别描述。
_Avoid_: 将上传完成直接称为部署成功

**会话接管**：调用者显式移交同一交互会话的输入控制权，原控制令牌失效。人工终端接管是可选诊断能力，不是正常产品流程。
_Avoid_: 将新开终端称为接管原会话
